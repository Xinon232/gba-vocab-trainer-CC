name: External dictionary and builder import verification
on:
  push:
    branches: [feature/v1.6-dictionary]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  host:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m pip install Pillow==12.3.0
      - run: bash tests/run_host_tests.sh --correctness-only
      - run: bash tests/run_dictionary_tests.sh --sanitize
  rom:
    runs-on: ubuntu-24.04
    container: devkitpro/devkitarm:20260610
    steps:
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
        with:
          repository: GValiente/butano
          ref: 77dcbcb3d8783596a9f333c64eedbccec77b05dc
          path: _butano
      - name: Build normal ROM from this source revision
        env:
          DEVKITPRO: /opt/devkitpro
          DEVKITARM: /opt/devkitpro/devkitARM
        run: |
          make -j2 LIBBUTANO="$GITHUB_WORKSPACE/_butano/butano"
          python3 tests/check_entry_memory.py
      - uses: actions/upload-artifact@v4
        with:
          name: gbavocab-rom
          path: |
            gbavocab.gba
            gbavocab.elf
          if-no-files-found: error
  package:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-2022, ubuntu-22.04]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m pip install PyInstaller==6.16.0 Pillow==12.3.0
      - run: python tests/test_dictionary_builder.py
      - run: python tests/test_builder_app.py
      - run: python -m unittest discover -s tests -p test_import_review.py
      - name: Package Windows PE and test actual executable
        if: runner.os == 'Windows'
        run: |
          python tests/test_import_review_gui.py
          python tests/test_builder_gui.py --out builder-dist/verification
          python builder/package.py
      - name: Package Linux executable and test actual executable
        if: runner.os == 'Linux'
        run: |
          xvfb-run -a python tests/test_import_review_gui.py
          xvfb-run -a python tests/test_builder_gui.py --out builder-dist/verification
          xvfb-run -a python builder/package.py
      - uses: actions/upload-artifact@v4
        with:
          name: dictionary-builder-${{ runner.os }}
          path: |
            builder-dist/gbavocab-builder-*.zip
            builder-dist/verification/*.json
            builder-dist/verification/*.png
            builder-dist/verification/*.dict
          if-no-files-found: error
